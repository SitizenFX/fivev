--- a/net/minecraft/client/renderer/entity/LivingEntityRenderer.java
+++ b/net/minecraft/client/renderer/entity/LivingEntityRenderer.java
@@ -73,6 +_,7 @@
     }
 
     public void submit(S p_427824_, PoseStack p_423787_, SubmitNodeCollector p_424901_, CameraRenderState p_422963_) {
+        if (net.minecraftforge.client.event.ForgeEventFactoryClient.onRenderLivingPre(p_427824_, this, p_423787_, p_424901_, p_422963_)) return;
         p_423787_.pushPose();
         if (p_427824_.hasPose(Pose.SLEEPING)) {
             Direction direction = p_427824_.bedOrientation;
@@ -108,6 +_,7 @@
 
         p_423787_.popPose();
         super.submit(p_427824_, p_423787_, p_424901_, p_422963_);
+        net.minecraftforge.client.event.ForgeEventFactoryClient.onRenderLivingPost(p_427824_, this, p_423787_, p_424901_, p_422963_);
     }
 
     protected boolean shouldRenderLayers(S p_360804_) {
@@ -286,6 +_,7 @@
         p_363057_.isFullyFrozen = p_368665_.isFullyFrozen();
         p_363057_.isBaby = p_368665_.isBaby();
         p_363057_.isInWater = p_368665_.isInWater();
+        p_363057_.isInWater = p_368665_.isInWaterOrSwimmable();
         p_363057_.isAutoSpinAttack = p_368665_.isAutoSpinAttack();
         p_363057_.hasRedOverlay = p_368665_.hurtTime > 0 || p_368665_.deathTime > 0;
         ItemStack itemstack = p_368665_.getItemBySlot(EquipmentSlot.HEAD);
@@ -309,6 +_,7 @@
     }
 
     protected void extractAdditionalHitboxes(T p_392957_, Builder<HitboxRenderState> p_396936_, float p_397822_) {
+        super.extractAdditionalHitboxes(p_392957_, p_396936_, p_397822_);
         AABB aabb = p_392957_.getBoundingBox();
         float f = 0.01F;
         HitboxRenderState hitboxrenderstate = new HitboxRenderState(
